# 折叠内容批注恢复修复记录

日期：2026-09-10。研究数据仍为 preview-13。本记录覆盖本地实现、浏览器验收与后续发布准备；公开部署结果另以发布回执为准，不表示完整互动验收通过。

## 已实现

批注保存与所选区域相交的折叠面板状态、内容指纹和可核对的身份。面板身份绑定页面、国家和稳定内容编号；没有编号的面板还绑定其语义内容。编号子块独立比较，表格行排序不会仅因排列顺序改变而破坏面板身份。同名或重复面板不按屏幕位置、数组下标猜测。

只有显式打开批注、分享链接或恢复草稿时才还原面板；普通位置刷新不改动读者的折叠选择。全部相关面板核对通过后才一起应用，某处改写、删除或歧义时不先恢复其余半个选区。隐藏内容与实质改写分别返回状态，隐藏时不使用浏览器可能残留的文字矩形。

旧文字和图片锚点没有展开记录时，先核对完整指纹与唯一文字/图片，再从实际匹配节点找到必要的祖先面板。对于不能通过面板恢复的样式隐藏，继续显示隐藏状态，不猜位置。旧的所有类型和历史错误锚点尚未全部验收。

跨越关闭面板选择可见文字时，按连续可见片段保存；不会把中间隐藏正文混入选中文字。原有字素、图片、父子目标与跨组件去重逻辑保持，草稿及Issue结构化标记保留新增展开状态。

追加修复：监听内容及根节点祖先的样式、可见性和语义属性变化，保留布局但隐藏内容时也会刷新位置；批注浮层自身及图表生成的悬浮层不触发内容刷新循环。图片与图表同时检查祖先的 content-visibility，避免保留过时矩形。

## 已取得的证据

- 506项本地测试、30个文件与类型检查通过，包括提交、D1保存及从模拟GitHub结构化标记重建展开状态。
- 桌面真实浏览器的25项折叠恢复检查通过：嵌套、旧文字/图片、关闭摘要、同名但无关面板、年份/国家不符、改写/删除、窄宽度加大字、跨组件、图片、表格排序、CSS隐藏及恢复。
- 既有48项真实浏览器DOM定位检查通过，包括父子目标、重复内容拒绝、图片、空白和Unicode字素。合计73项检查；窄容器测试不等同于手机整页验收。
- 修复前的实际交通泊位006草稿成功恢复：原“期初投入包含设备、场”选区重新显示为144×17，桌面位置x=32、y=139.515625；现金流面板已展开，正文“折叠内容定位检查：恢复后应重新展开现金流说明。”保持，没有原文变更误报。
- 用户重新打开浏览器后，再次恢复该旧草稿；实际点击关闭现金流面板后，编辑卡显示“选区内容当前折叠或隐藏，未判定为原文变更”，四个调整手柄消失。点击“展开并定位”重新打开面板并恢复手柄。
- 390×844 浏览器视口下，旧草稿的底部浮层为390×430.234375，页面宽度390，无横向溢出。
- 同一实际页新建手机框选，将280×160调整为309×199；快照天然尺寸309×199，视觉检查仅包含选中的正文和空白。填写评论后收起、重载、从草稿菜单恢复，现金流面板重新展开，评论原样保留，快照仍309×199。恢复后的标记按语义片段重新包围文字，不要求与原像素裁切边界相同。
- 手机25项折叠与48项既有DOM检查通过。补充14项图表、空白、重复面板、内容可见性和观察器检查后，手机39项通过，桌面最终39+48=87项通过。随后补做手机重复运行时浏览器超时，未把该次未返回结果记为通过；最后的测试组件改动仅将两个DOM插入方法替换为等价方式以适配类型声明。
- 最终506项本地测试、30个文件及类型检查通过，测试耗时102.02秒。上一提交25c3a33的CI 34387542222已成功；本次新增可见性修复的CI与发布结果单独记录。

## 尚待完成

第二次连接中断时，应用内浏览器不可用；发现列表仅有Chrome，没有改用其他浏览器绕过原定检查。恢复默认视口和回到首页的操作未取得完成回执，不能声称已恢复用户浏览状态。

完整键盘聚焦路径、旧的无文字整块选区、真实ECharts在折叠面板内的异步布局、更多设备和全部异常矩阵仍需继续验收。图表数据点检查使用浏览器中的语义柱状图测试夹具，不冒充全部ECharts场景。临时QA页面在提交前删除，测试组件保留在tests目录。

GitHub App、R2与真实作者互动仍未配置完整，不用本地模拟代替真实身份、权限或双向同步证明。
