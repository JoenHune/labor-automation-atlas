# 快照生成期间的评论保留修复

2026-09-10。研究仍为 preview-13，没有新增或冻结任务。

在公开泊位006任务页的390×844视口中，调整选区后立即填写评论，快照完成时正文被清空；DOM与截图均确认。原因是异步生成快照后，用生成前的草稿对象覆盖了期间的输入。

修复后，新选区只合并到当前同一草稿，保留最新正文，包括追加和主动清空。已切换的草稿、国家、页面、回复或已提交正文不会接收迟到的选区；页面更换使未完成的捕获失效。重新捕获先清除旧选区和快照，失败时保留评论并要求重新选择，避免将旧快照作为新框选提交。

生成快照时可以继续输入。真正发起提交时先固定正文、回复对象和处理原因，前置登录失败后恢复编辑。页面提示快照正在生成，帮助区分两个阶段。

516项检查、31个文件及类型检查通过，新增10项延迟完成/失败检查。实际手机页面已分别验证新建和调整期间输入：当时按钮仍显示处理中、输入框可编辑；完成后文字保持。重载后完整追加正文、278×158快照及四个手柄恢复，页面宽390，没有横向溢出。登录尚未配置的错误也保留正文并恢复编辑。

重新连接后，手机39项折叠检查与48项既有DOM检查均通过。公开修复前的草稿在重新填写后也成功重载，311×255快照、展开的现金流内容和评论保持。正常浏览尺寸已恢复为951×908，并回到中国行业全景。详细记录见同名JSON。

真实身份、权限、授权过期、R2异常与GitHub双向同步仍待服务配置后验收；此次未发送GitHub评论。公开部署和发布后交互另记回执。

公开修复提交daef4a678814faf174b89a2af9fe2ebe85424b4b已通过CI 34433710637及Pages 34433710623。发布后手机页面也完成新建、调整时输入和重载验证：两次输入均发生在快照生成期间，正文保持，重载后299×183快照和四个手柄恢复。临时QA路径404，公开任务导出与本地相同。最后恢复默认951×908尺寸及原本地中国页；详见annotation-draft-capture-publication-receipt.json。
